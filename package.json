{
  "name": "testauth",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "now-build": "mkdir -p dist && cp test.html index.html dist/ && ls -al dist/ && true"
  },
  "dependencies": {
    "cookie": "^0.3.1",
    "urlencoded-body-parser": "^2.0.1"
  }
}
